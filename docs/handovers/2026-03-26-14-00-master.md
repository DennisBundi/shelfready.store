# Handover: master

**Date:** 2026-03-26 14:00
**Branch:** master
**Session goal:** Redesign landing page hero to split layout with drag-to-compare slider, fix AI image generation pipeline

## Completed Work

- **Hero redesign**: Split two-column layout (text+form left, drag-to-compare slider right) replacing centered hero
- **CompareSlider component**: `src/components/CompareSlider.tsx` — CSS clip-path slider with keyboard/ARIA support
- **BeforeAfterSection**: Rewritten as extensible `pairs`-array gallery (`src/components/BeforeAfterSection.tsx`)
- **Real images**: `public/before.png` (mannequin store photo), `public/after.png` (AI-generated models — updated twice)
- **Supabase fixes**: `profiles` table created with trigger for auto-row on signup; RLS policies added to `product-images` storage bucket
- **AI generation**: Migrated from `@google/generative-ai` → `@google/genai` SDK; demo slot rollback on failure added
- **Webpack fix**: Dynamic import of `@google/genai` in `generate.ts` to prevent client bundle pollution

## Current State

- Tests: Not configured
- Build: Dev server running on localhost:3002
- Uncommitted changes: `.claude/settings.local.json` (modified), `src/app/api/debug-models/` (untracked — temp debug route), `supabase/` (untracked)

## Next Steps

- [ ] Enable Google Cloud billing on the project linked to the Gemini API key — current key still hitting free tier quota (limit: 0 for `gemini-2.5-flash-image`)
- [ ] Delete `src/app/api/debug-models/route.ts` (temp debug route, should not ship)
- [ ] Push commits to remote (`git push origin master`)
- [ ] Fix demo slot logic: currently claims slot before generation succeeds (rollback added on error, but worth a full review)

## Key Resources

- `src/app/actions/generate.ts` — AI generation server action, uses `@google/genai` with dynamic import
- `src/components/CompareSlider.tsx` — drag-to-compare slider component
- `src/components/HeroSection.tsx` — split hero layout
- `docs/plans/2026-03-25-hero-redesign.md` — implementation plan for the hero redesign
- Supabase project: `lmfxotadxhuocnpacznt.supabase.co`

## Decisions Made

- **Dynamic import for `@google/genai`**: Static import caused webpack "Cannot read properties of undefined (reading 'call')" error on all pages; dynamic import keeps it server-side only
- **`gemini-2.5-flash-image` model**: Confirmed available via ListModels API; previous model names (`gemini-2.0-flash-exp`, `gemini-2.0-flash-preview-image-generation`) all returned 404
- **Hugging Face attempted then abandoned**: `timbrooks/instruct-pix2pix` no longer has an inference provider on HF free tier

## Blockers & Warnings

- **Gemini billing not active**: API key returning quota=0 for `gemini-2.5-flash-image`. Need a key from a Google Cloud project with billing enabled (not the default AI Studio free project). Instructions: console.cloud.google.com → link billing account → get API key from that project in aistudio.google.com
- **Demo slot consumed**: Run `UPDATE profiles SET demo_used = FALSE WHERE email = 'your@email.com';` in Supabase SQL editor before each test generation
